name: Hash bank parts

on:
  push:
    branches:
      - feat/10-bancos-publicos-autoria

permissions:
  contents: write

jobs:
  hash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hash fragments
        shell: bash
        run: |
          sha256sum .bank-staging/part-[0-9][0-9] | sort -k2 > .bank-staging/part-sha256.txt
      - name: Commit hashes
        shell: bash
        run: |
          if git diff --quiet -- .bank-staging/part-sha256.txt; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .bank-staging/part-sha256.txt
          git commit -m "Actualizar huellas de fragmentos temporales"
          git push
